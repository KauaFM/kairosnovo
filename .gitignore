# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
build/
.vite/

# Environment (NEVER commit real secrets)
.env
.env.local
.env.*.local
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lint_output.txt

# Testing
coverage/
.nyc_output/

# Supabase local state
supabase/.branches/
supabase/.temp/

# Capacitor / mobile builds
# Projeto Android: VERSIONADO (o CI precisa dele para compilar o AAB).
# Fora do git só o que é gerado ou secreto:
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
# ⚠️ NUNCA versionar: quem tiver o .jks + a senha publica no seu nome
android/keystore.properties
android/*.jks
android/*.keystore
ios/
.gradle/
*.keystore
*.jks

# Misc
*.local
.turbo/

# Claude Code worktrees
.claude/worktrees/

# MCP config (pode conter chave do Stripe — nunca commitar)
.mcp.json
.claude/settings.local.json

.vercel
